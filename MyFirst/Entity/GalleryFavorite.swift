import UIKit

class GalleryFavorite {
    let key: String
    let title: String
    let image: UIImage
    
    init(key: String, title: String, image: UIImage) {
        self.key = key
        self.title = title
        self.image = image
    }
}
